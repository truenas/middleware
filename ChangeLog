o Add FibreChannel support.

o Fix a bug that was preventing proper operation of AD integration in HA
  units.

o Fix a GUI traceback that could occur when editing various network
  interfaces that contained a VIP (Virtual IP)

o Fix an alert that would occur on single node Z-series heads that
  stated "The enclosure is critical"

o Fix a spurious alert that would show bad voltages on the Z-series
  chassis enclosure.  This alert is purely a cosmetic bug, no voltage
  irregularities have been detected on any units exhibiting this alert.

o Fix a bug that could cause python core dumps to be logged when
  LDAP SSL or TLS support was enabled.
