o Add Fibre Channel Virtual Port support.

o Add performance reporting for Fibre Channel ports.

o Update the CA bundle included in TrueNAS.

o Add support for > 16 NFS groups.

o Remove a 60 second timeout per lagg when booting or
  restarting networking.

o Fix a bug in the NFS server that could leave the
  NFS service non-responsive.

o Fix a bug that prevented the factory reset CLI option
  from working.

o Add support for binding the SSH service to specific
  interfaces.

o Fix a bug that prevented switching between LDAP and
  AD directory services.

o Add X-*NAS-Host into update available emails.
