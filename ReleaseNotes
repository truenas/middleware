Release Notes for FreeNAS 9.2.1.9-RELEASE

o Fix a bug preventing Directory Server mode from working.

o Fix a memory leak in ZFS that is triggered by having
  a compressed dataset and an L2ARC device.

o Preserve the Samba SID across reboots and upgrades.

o Fix two problems in the config file generator for CTL:
  1) Unbreak device extents when using physical devices
     or multipath devices.
  2) Unbreak the case when target auth or discover auth
     is set to Auto.

o Fix a priviledge escalation.
