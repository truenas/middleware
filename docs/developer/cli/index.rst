FreeNAS CLI
===========

.. toctree::
   :maxdepth: 1

   Writing CLI plugins <plugins>

FreeNAS 10 introduces it's own Command Line Interface which can (and should) be used instead
of UNIX shell.

CLI is Python program, consisting of basic REPL (read-eval-print-loop) command interpreter,
plugin framework and set of plugins providing actual functionality.