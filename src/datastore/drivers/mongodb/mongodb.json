{
	"Label": "org.mongodb.mongod",
	"ProgramArguments": [
		"/usr/local/bin/mongod",
		"--config", "${driverdir}/mongod.conf",
		"--logpath", "${dbdir}/mongod.log",
		"--dbpath", "${dbdir}/freenas.db",
		"--quiet"
	],
	"RunAtLoad": true,
	"KeepAlive": true
}
