
Installation Instructions

First, make sure you have the following dependancies installed:

devel/xdg-utils

After loading those, simply run "./install.sh"

The default localbase is "/usr/local", if you wish to use something different, then
run "./install.sh <newlocalbase>"

