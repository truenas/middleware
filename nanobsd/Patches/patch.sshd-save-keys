Index: etc/rc.d/sshd
===================================================================
--- etc/rc.d/sshd	(revision 208655)
+++ etc/rc.d/sshd	(working copy)
@@ -86,6 +86,9 @@
 	    ! -f /etc/ssh/ssh_host_rsa_key ]; then
 		user_reseed
 		run_rc_command keygen
+		if [ -x /root/save_sshkeys ]; then
+			/root/save_sshkeys
+		fi
 	fi
 }
 
